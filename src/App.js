import app from 'firebase/app';
import { getAuth } from "firebase/auth";
import './App.css';

function App() {
  const auth = getAuth(app);

  return (
    <div>

    </div>
  );
}

export default App;
